# ACADILOVABLE

Security scanner for applications built with vibe-coding platforms and AI agents. Performs automatic stack fingerprinting, JavaScript bundle static analysis, endpoint discovery, active security testing, and generates an interactive HTML report with finding validation.

**Author:** Thiago Muniz  
**LinkedIn:** [linkedin.com/in/tmtic](https://www.linkedin.com/in/tmtic/)  
**Instagram:** [@eutmtic](https://www.instagram.com/eutmtic)

---

## Supported Platforms

ACADILOVABLE detects and analyzes applications generated by the following platforms:

### Vibe-Coding / AI Agents

| Platform | Typical stack detected |
|---|---|
| Lovable | Supabase + React + Vite |
| v0.dev | Next.js + Vercel + Prisma/Drizzle |
| Bolt.new | Firebase/Supabase + React/Vue |
| Replit Agent | Flask/FastAPI or Node.js + PostgreSQL |
| Cursor | Any stack |
| Windsurf (Codeium) | Any stack |
| Claude Code | Any stack |
| GPT-Pilot | FastAPI + PostgreSQL |
| Devin (Cognition Labs) | Any stack |
| Aider | Any stack |
| GitHub Copilot Workspace | Any stack |
| Plandex | Any stack |
| Melty | Any stack |
| AllHands / OpenDevin | Python + Docker |
| Sourcegraph Cody | Any stack |
| Amazon Q Developer | AWS + any stack |
| Supermaven | Any stack |

### Backends & BaaS

Supabase · Firebase · Convex · PocketBase · Neon · PlanetScale · Express · FastAPI · Flask · Django · Hono · Elysia

### ORMs

Prisma · Drizzle · TypeORM · SQLAlchemy · tRPC

### Auth Providers

Supabase Auth · Firebase Auth · Clerk · Auth0 · NextAuth.js · Lucia · Better-Auth

### Hosting

Vercel · Netlify · Railway · Render · Fly.io · Cloudflare Pages · Replit

---

## Installation

```bash
git clone https://github.com/tmtic/acadilovable.git
cd acadilovable
chmod +x acadilovable.sh
```

Check dependencies:

```bash
./acadilovable.sh --check-deps
```

Auto-install missing dependencies (detects OS automatically):

```bash
./acadilovable.sh --install-deps
```

| OS | Package manager used |
|---|---|
| Ubuntu / Debian | `apt-get` |
| CentOS / RHEL / Fedora | `dnf` or `yum` |
| Arch Linux | `pacman` |
| macOS | `brew` (requires [Homebrew](https://brew.sh)) |
| Alpine Linux | `apk` |

**Required:** `bash 4+` · `curl` · `python3 3.8+` · `grep` · `sed` · `awk`  
**Optional:** `jq` · `git`

---

## Usage

```bash
./acadilovable.sh -u <URL> [OPTIONS]
```

### Flags

#### Target

| Flag | Description |
|---|---|
| `-u, --url <URL>` | Target application URL **(required)** |

#### Scan profiles

| Flag | Behavior |
|---|---|
| `--quick` | Passive analysis only — no active HTTP probing |
| `--full` | Deep scan — crawl depth 5, 100 rows per table, full fuzzing |
| *(default)* | All phases with moderate depth and timeouts |

#### Scan control

| Flag | Description |
|---|---|
| `--skip-download` | Reuse previously downloaded JS assets (faster re-scan) |
| `--no-probe` | Skip all active HTTP probing entirely |
| `--timeout <sec>` | Per-request HTTP timeout (default: 12s) |
| `--detect` | Only detect if the app was built with vibe-coding (fast, no full scan) |

#### Optional active tests

Two modules are disabled by default to avoid generating noise in the target's WAF, SIEM and IDS:

| Flag | What it enables |
|---|---|
| `--enable-injection` | SQLi, NoSQLi, SSTI, CMDi — tests parameters on dynamic endpoints |
| `--enable-ssrf` | SSRF against cloud metadata (AWS IMDS, GCP, Azure) |
| `--aggressive` | Enables injection + SSRF + full scan |

#### Output

| Flag | Description |
|---|---|
| `-q, --quiet` | Show only vulnerabilities and final summary |
| `-v, --verbose` | Detailed debug output for each test |
| `--json` | Write machine-readable summary to `scan.json` |
| `--out <dir>` | Custom output directory |

#### Validation & dependencies

| Flag | Description |
|---|---|
| `--validate [dir]` | Generate `validate_findings.sh` from an existing scan output |
| `--check-deps` | Check if all prerequisites are installed |
| `--install-deps` | Auto-install missing prerequisites |
| `-h, --help` | Show full help |

---

## Examples

```bash
# Lovable app (Supabase + React)
./acadilovable.sh -u https://myapp.lovable.app

# v0.dev or Vercel app (Next.js)
./acadilovable.sh -u https://myapp.vercel.app

# Bolt.new app (Firebase)
./acadilovable.sh -u https://myapp.web.app

# Replit app (any stack)
./acadilovable.sh -u https://myapp.replit.app

# Detect only — is this a vibe-coding app?
./acadilovable.sh -u https://target.com --detect

# Passive scan only (no active requests)
./acadilovable.sh -u https://target.com --quick

# Full scan with all active tests enabled
./acadilovable.sh -u https://target.com --aggressive

# Re-scan reusing existing assets, JSON output
./acadilovable.sh -u https://target.com --skip-download --json

# Generate validator from a previous scan
./acadilovable.sh --validate output/myapp.lovable.app

# Check installed dependencies
./acadilovable.sh --check-deps
```

---

## How it works

The scan runs in 6 sequential phases.

### Phase 1 — Asset discovery

Locates and downloads the application's JavaScript bundle. First attempts via `sw.js` (service worker precache), then falls back to `index.html` script tag scraping. The bundle is the main compiled file containing all SPA code.

### Phase 2 — Bundle analysis

Statically analyzes the JavaScript bundle to extract:

- **Credentials:** Supabase URL and anon key, Firebase config object, Convex URL, Clerk publishable key, Stripe keys, AWS access keys, OpenAI keys, SendGrid, Mapbox, Pusher tokens
- **Database structure:** tables accessed via `.from()`, RPCs via `.rpc()`, Edge Functions, Storage buckets, query chains with filter analysis
- **API calls:** all `fetch()` and `axios()` calls found in the code with their HTTP methods
- **SPA routes:** navigation paths extracted from the router configuration
- **Source maps:** references to `.map` files that expose the original pre-build source code
- **Environment variables:** detection of `process.env.SECRET_KEY` and similar server-only vars in the client bundle

Any third-party API key found in the bundle is immediately reported as a finding.

### Phase 3 — Platform detection & discovery

Three tasks run in parallel:

**Stack fingerprinting** analyzes HTTP response headers, bundle content and URL patterns to identify the frontend framework, backend/BaaS, ORM, auth provider and hosting platform.

**Web crawler** recursively traverses the application (depth 3 on standard profile, 5 on `--full`), cataloging URLs, content-types, sizes and paths that appear interesting (admin, api, dashboard, config, debug, etc.).

**Sensitive file scanner** probes 70+ known sensitive paths — `.env`, `.git/HEAD`, `backup.sql`, `swagger.json`, `actuator/env`, `phpinfo.php`, `docker-compose.yml`, and many more — reporting any that are accessible with their content preview.

After discovery, the database schema is extracted via PostgREST OpenAPI (Supabase) or Swagger and a complete endpoint map is built.

### Phase 4 — Static security analysis

The JWT anon key found in the bundle is decoded and analyzed:
- Algorithm and role (`anon` vs `service_role`)
- Issue date and expiration (a token with no expiry is a finding)
- Attack variants generated: `alg=none`, role escalation to `service_role`, null signature

### Phase 5 — Active security testing

The most extensive phase. Tests run in the following order:

**Headers & CORS**

Verifies presence and quality of: CSP (with `unsafe-inline` and `unsafe-eval` analysis), HSTS (presence and `max-age` value), X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy.

Tests CORS by sending `Origin: https://evil.example.com` and checking whether the server reflects the origin, returns a wildcard `*`, or properly restricts it.

**Platform-specific scanners**

Activated based on the detected stack:

*Supabase:* unauthenticated access sweep on all mapped endpoints; IDOR with alternative UUIDs; PostgREST OR filter bypass (`?or=(id.gt.0)`); CSV export without auth; JWT attacks (alg=none, role escalation); Storage bucket listing; rate limiting detection on login/signup/recover.

*Firebase:* unauthenticated Firestore read via REST API; Realtime Database with `.json` suffix; Storage bucket listing; weak password policy on signup; email enumeration via `accounts:createAuthUri`.

*Next.js/Vercel:* build ID extraction and `/_next/data/{buildId}/*.json` enumeration; exposed source maps; server-only env vars in client bundle; NextAuth `/api/auth/session` without authentication; unauthenticated tRPC endpoints.

*GraphQL:* introspection enabled in production; no query depth limit (DoS risk); batch query acceptance without limit (DoS risk); SQLi through GraphQL variables.

**Intelligent endpoint classification**

Before any active test runs, the classifier analyzes all endpoints and assigns scores per attack vector:

- `file-serving` (high traversal score): routes like `/download`, `/file`, `/doc`, parameters `?file=`, `?path=`
- `api-dynamic` (high injection score): routes like `/search`, `/users`, `/query` with GET/POST parameters
- `url-fetching` (high SSRF score): routes like `/webhook`, `/proxy`, `/import`, parameters `?url=`, `?redirect=`
- `supabase-rest` (score -100 for all active tests): handled by the dedicated Supabase scanner
- `static` (skip entirely): `.js`, `.css`, `.png`, etc.

Each scanner only receives relevant candidates, with a time budget per test (60s traversal, 90s injection, 45s SSRF) and early-exit when consecutive misses accumulate on low-score endpoints.

**OWASP Top 10 (A01–A10)**

Systematically tests each category with CVSS score, MITRE ATT&CK technique ID and WSTG test ID mapped to every finding.

Covers: directory listing, `.git/HEAD` exposure, `.env` exposure, verbose error pages with stack traces, server version disclosure, cookies without HttpOnly/Secure/SameSite, external scripts without SRI, HTTP TRACE method, reflected XSS probe, open redirect (distinguishes server-side from client-side SPA behavior).

**Injection tests (optional — `--enable-injection`)**

SQLi via GET params and POST body (payloads: `' OR '1'='1`, `1 UNION SELECT NULL--`, single quote for error triggering); SSTI by detecting `{{7*7}}` reflected as `49`; CMDi with `; id`, `$(id)`; NoSQLi with `{"$ne": null}`.

**SSRF tests (optional — `--enable-ssrf`)**

Probes URL-like parameters (`url`, `redirect`, `webhook`, `proxy`, `image`, `src`, `feed`) with cloud metadata targets: AWS IMDS `169.254.169.254/latest/meta-data/iam/`, GCP metadata, Azure IMDS, `localhost`, `127.0.0.1`.

### Phase 6 — Database & user intelligence

Runs only when Supabase is detected:

**Database query:** performs SELECT on all discovered tables using the anon key, reporting how many rows return and detecting PII columns (email, phone, cpf, address, full_name, birth_date, etc.). Tests CSV export on each table.

**User enumeration:** probes the admin API (`/auth/v1/admin/users`), tables with user-related names (`profiles`, `users`, `accounts`, `members`), user enumeration via password reset (different responses for existing vs non-existing emails), exposed audit log.

**RPC probing:** tests 16 potentially dangerous RPC name patterns (`exec_sql`, `run_query`, `pg_read_file`, `pg_ls_dir`, etc.) and checks for direct access to `information_schema` via PostgREST.

---

## Output files

All results are saved to `output/<domain>/`:

| File | Contents |
|---|---|
| `report.html` | Full interactive dashboard — dark mode, filterable tables, OWASP findings, endpoint map, crawler results, database data |
| `findings.txt` | Sequential log of all findings — `VULN:`, `MISCONFIG:`, extracted variables |
| `validate_findings.sh` | Auto-generated script to validate each finding with real curl commands |
| `owasp.json` | OWASP findings with CVSS score, MITRE ATT&CK ID and WSTG ID |
| `platform.json` | Detected stack — platform, frontend, backend, auth, hosting, credentials found |
| `endpoints.txt` | `METHOD\|URL\|SOURCE\|TAG` for every mapped endpoint |
| `classified.json` | Per-endpoint attack vector scores (traversal, injection, ssrf, xss) |
| `apifuzz.json` | API fuzzer results — accessible endpoints, PII detected, server errors |
| `db_dump.json` | Data extracted from open tables with row counts and PII columns |
| `users.json` | User data found during enumeration |
| `crawl.json` | Crawled URLs with HTTP status, content-type and response size |
| `sensitive_files.json` | Accessible sensitive paths with content preview |
| `js_parse.json` | Bundle analysis data — tables, RPCs, routes, secrets, env vars, chains |
| `curl_commands.sh` | Ready-to-run curl commands for every mapped endpoint |
| `scan.json` | Machine-readable summary (generated with `--json`) |
| `py/` | 20 Python helpers generated at scan start |

The `validate_findings.sh` is also placed in the **root directory** (same folder as `acadilovable.sh`) for immediate access.

---

## Finding validation

At the end of every scan, `validate_findings.sh` is generated automatically, customized with the real findings, URLs, API keys and table names discovered:

```bash
# Generated automatically during a scan
./acadilovable.sh -u https://myapp.lovable.app
bash validate_findings.sh

# Generated from an existing scan output
./acadilovable.sh --validate output/myapp.lovable.app
bash validate_findings.sh
```

Each block in the validator:
1. Shows the exact curl command being executed
2. Runs it with the real discovered credentials
3. Classifies the result:

| Result | Meaning |
|---|---|
| `[VP CONFIRMADO]` | Confirmed vulnerability with real evidence |
| `[FALSO POSITIVO]` | Expected behavior, not a vulnerability |
| `[MANUAL]` | Requires browser verification (e.g. client-side redirects in SPAs) |

---

## HTML report

The generated `report.html` includes:

- **Metrics panel** — vulnerability count with severity badge, misconfiguration count, endpoint count, crawled URLs, sensitive files, database tables, OWASP findings
- **Detected stack** — vibe-coding platform, frontend, backend, auth provider, hosting
- **Vulnerabilities and misconfigs** — full list with descriptions
- **OWASP table** — each finding with color-coded severity, OWASP category, MITRE ATT&CK ID, WSTG ID and CVSS score
- **Crawler results** — filterable table of all visited URLs with status, content-type and size
- **Sensitive files** — accessible paths with content preview
- **Endpoint map** — filterable table with HTTP method, URL, source and tag
- **Database** — discovered tables, extracted data, SPA routes
- No external dependencies — fully self-contained single HTML file

---

## Security coverage

### OWASP Top 10 (2021)

| Category | What is tested |
|---|---|
| A01 Broken Access Control | Unauthenticated access, IDOR with alternative IDs, filter bypass, user enumeration |
| A02 Cryptographic Failures | HTTP without HTTPS redirect, weak/missing HSTS, mixed content |
| A03 Injection | SQLi (GET/POST), NoSQLi, SSTI detecting `7*7=49`, CMDi, reflected XSS |
| A04 Insecure Design | Stack traces in error pages, verbose errors, test endpoints in production |
| A05 Security Misconfiguration | `.git/HEAD`, `.env`, directory listing, HTTP TRACE, server version disclosure |
| A06 Vulnerable Components | Framework version in bundle, server header disclosure |
| A07 Auth Failures | Cookies without HttpOnly/Secure/SameSite, missing rate limiting, JWT attacks |
| A08 Software Integrity | External scripts without SRI, exposed source maps |
| A09 Security Logging | Missing rate limiting on authentication endpoints |
| A10 SSRF | AWS IMDS, GCP metadata, Azure IMDS, internal services |

### OWASP WSTG

WSTG-INFO-01 · WSTG-CONFIG-02 · WSTG-CONFIG-04 · WSTG-CONFIG-06 · WSTG-CONFIG-11 · WSTG-CRYPST-01 · WSTG-ERRH-01 · WSTG-SESS-02 · WSTG-CLNT-01 · WSTG-CLNT-04 · WSTG-CLNT-09 · WSTG-CLNT-10 · WSTG-CLNT-11 · WSTG-CLNT-13

### Supabase-specific checks

RLS bypass · PostgREST OR filter bypass · IDOR per table · JWT alg=none · JWT role escalation · service_role key in bundle · CSV export without auth · admin users API exposure · audit log exposure · storage bucket listing · user enumeration via password reset · dangerous RPCs (exec_sql, pg_read_file, etc.) · information_schema access via PostgREST

### Firebase-specific checks

Firestore without security rules · Public Realtime Database · Public Storage bucket · Weak password policy · Email enumeration via createAuthUri

### Next.js / Vercel-specific checks

Build ID enumeration · `/_next/data/` route exposure · Source map exposure · Server-only env vars in client bundle · NextAuth session leak · Unauthenticated tRPC endpoints

### GraphQL-specific checks

Introspection enabled in production · No query depth limit (DoS) · Batch query acceptance (DoS) · SQLi via GraphQL variables

---

## Python helpers

The script generates 20 `.py` helper files in `output/<domain>/py/` at scan start:

| Helper | Function |
|---|---|
| `precache.py` | Extracts asset URLs from `sw.js` precache manifest |
| `idx.py` | Extracts assets from `index.html` script tags |
| `extract.py` | Deep JS bundle parser — tables, RPCs, routes, secrets, env vars, query chains |
| `platform.py` | Stack fingerprinting via HTTP headers and bundle content |
| `jwt.py` | JWT decoder and security analyzer |
| `jwt_atk.py` | JWT attack variant generator (alg=none, role escalation, no signature) |
| `schema.py` | OpenAPI/PostgREST schema parser |
| `crawl.py` | Recursive web crawler with interesting path detection |
| `filescan.py` | Sensitive file scanner — 70+ paths, 25 parallel threads |
| `dbquery.py` | Supabase table query engine with PII column detection |
| `firebase.py` | Firebase/Firestore security scanner |
| `nextjs.py` | Next.js/Vercel security scanner |
| `graphql.py` | GraphQL security scanner |
| `apifuzz.py` | Generic REST API fuzzer — filters 404, reports 200/301/403/500 |
| `classify.py` | Endpoint classifier with per-vector attack scoring |
| `inject.py` | SQLi/SSTI/CMDi injection tester with time budget and early-exit |
| `traversal.py` | Path traversal tester — only runs on file-serving endpoints |
| `ssrf.py` | SSRF detector with cloud metadata targets |
| `owasp.py` | OWASP Top 10 + WSTG with CVSS, MITRE ATT&CK and WSTG IDs |
| `gen_validate.py` | Generates the customized validate_findings.sh from scan results |

---

## Standards & references

- [OWASP Top 10 (2021)](https://owasp.org/Top10/)
- [OWASP Web Security Testing Guide (WSTG)](https://owasp.org/www-project-web-security-testing-guide/)
- [MITRE ATT&CK Enterprise](https://attack.mitre.org/)
- [CVSS v3.1 Specification](https://www.first.org/cvss/v3-1/)
- [Supabase Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [PostgREST API Reference](https://postgrest.org/en/stable/)
- [Firebase Security Rules](https://firebase.google.com/docs/rules)

---

## Legal notice

This tool is intended exclusively for **authorized security testing**.

Only use it on systems for which you have explicit permission to test. Unauthorized testing may violate computer crime laws. The author is not responsible for any misuse of this tool.

---

## Author

**Thiago Muniz**

- LinkedIn: [linkedin.com/in/tmtic](https://www.linkedin.com/in/tmtic/)
- Instagram: [@eutmtic](https://www.instagram.com/eutmtic)
